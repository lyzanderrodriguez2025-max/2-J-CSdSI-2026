def height(n, m):
    if n == 0 or m == 0:
        return 0

    n = min(n, m)

    result = 0
    c = 1

    for i in range(1, n + 1):
        c = c * (m - i + 1) // i
        result += c

    return result